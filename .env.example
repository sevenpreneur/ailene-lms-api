# Neon Postgres (production branch) — direct connection, not the -pooler variant
SPRING_DATASOURCE_URL=jdbc:postgresql://<host>/<database>?sslmode=require
SPRING_DATASOURCE_USERNAME=<role>
SPRING_DATASOURCE_PASSWORD=<password>

# Overridden by Railway at deploy time; defaults to 8080 locally
PORT=8080
